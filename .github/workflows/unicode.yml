name: Unicode
on:
  push:
    branches:
      - 'master'
  pull_request:
  workflow_dispatch:

jobs:
  test-only:
    runs-on: ubuntu-latest
    steps:
      - name: Test  🛎
        run: |
          echo 'Test unicode'
          mkdir test-uni
          ls -a 
          cp -f unicode/test.sh test-uni/test.sh
          cd test-uni
          ls -a
          ./test.sh